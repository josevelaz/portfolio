import React from "react"
import projectStyles from "./projects.module.scss"
import Card from "../Card/Card"

export const Project = props => {
  return (
    <div className="main">
      <Card containerStyle={{ width: "50vw" }}>Hello</Card>
    </div>
  )
}
